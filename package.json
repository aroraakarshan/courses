{
  "name": "courses",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "git submodule update --init --recursive && node build.mjs && astro build",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.3.0",
    "astro": "^5.0.0",
    "hono": "^4.12.16"
  }
}
