import{C as e,I as t,L as n,N as r,R as i,X as a,d as o,x as s}from"../chunks/DI_erTYQ.js";import{c}from"../chunks/C2uaKz3z.js";import"../chunks/ibwe1TAv.js";import{t as l}from"../chunks/CVfHkc3z.js";import{t as u}from"../chunks/u_6yPRx5.js";import{t as d}from"../chunks/mvW8Je6C.js";var f=e(`<h1>Handling YAML Errors in Python</h1> <!> <p>Real files are not always clean. A colleague fat-fingers a tab instead of a space, a script points at the wrong folder, or a forgotten quote breaks a value. When this happens, <code>yaml.safe_load()</code> will stop your program with an error — <em>unless</em> you handle it gracefully.</p> <p>This lesson shows the two errors you will meet in practice and the small amount of Python needed to handle them like a professional.</p> <hr/> <h2>The two errors you will actually hit</h2> <table><thead><tr><th>Error</th><th>What it means</th><th>Typical cause</th></tr></thead><tbody><tr><td><code>yaml.YAMLError</code></td><td>The file exists, but its contents aren’t valid YAML</td><td>tab indentation, missing colon, unbalanced quotes</td></tr><tr><td><code>FileNotFoundError</code></td><td>The file does not exist at the given path</td><td>wrong folder, typo in the file name</td></tr></tbody></table> <p>There are others, but these two cover > 95 % of real cases for config files in semiconductor flows.</p> <hr/> <h2>What <code>YAMLError</code> looks like</h2> <p>Say your <code>chip_config.yaml</code> has a tab on line 5 (which YAML forbids). Run <code>yaml.safe_load()</code> on it and Python stops with something like this:</p> <pre><code>yaml.scanner.ScannerError: while scanning for the next token
found character '\\t' that cannot start any token
  in "chip_config.yaml", line 5, column 1</code></pre> <p>The important parts:</p> <ul><li><strong><code>line 5, column 1</code></strong> — exactly where to look in your editor</li> <li><strong><code>found character '\\t'</code></strong> — the actual problem (a tab)</li></ul> <p>The error message is ugly, but it is very <em>precise</em>. Lesson 08 covered the same errors from the validator side — Python’s <code>yaml</code> library uses the same machinery, so you learn one set of messages and they apply everywhere.</p> <hr/> <h2>The safe pattern</h2> <p>Here is the idiomatic Python way to read a YAML file while handling both errors. Memorise the shape, not the details:</p> <pre class="shiki github-light" style="background-color:#fff;color:#24292e" tabindex="0"><code><span class="line"><span style="color:#D73A49">import</span><span style="color:#24292E"> yaml</span></span>
<span class="line"></span>
<span class="line"><span style="color:#D73A49">try</span><span style="color:#24292E">:</span></span>
<span class="line"><span style="color:#D73A49">    with</span><span style="color:#005CC5"> open</span><span style="color:#24292E">(</span><span style="color:#032F62">'chip_config.yaml'</span><span style="color:#24292E">) </span><span style="color:#D73A49">as</span><span style="color:#24292E"> f:</span></span>
<span class="line"><span style="color:#24292E">        config </span><span style="color:#D73A49">=</span><span style="color:#24292E"> yaml.safe_load(f)</span></span>
<span class="line"><span style="color:#D73A49">except</span><span style="color:#24292E"> yaml.YAMLError </span><span style="color:#D73A49">as</span><span style="color:#24292E"> e:</span></span>
<span class="line"><span style="color:#005CC5">    print</span><span style="color:#24292E">(</span><span style="color:#032F62">"Your YAML file has a syntax error:"</span><span style="color:#24292E">)</span></span>
<span class="line"><span style="color:#005CC5">    print</span><span style="color:#24292E">(e)</span></span>
<span class="line"><span style="color:#005CC5">    exit</span><span style="color:#24292E">(</span><span style="color:#005CC5">1</span><span style="color:#24292E">)</span></span>
<span class="line"><span style="color:#D73A49">except</span><span style="color:#005CC5"> FileNotFoundError</span><span style="color:#24292E">:</span></span>
<span class="line"><span style="color:#005CC5">    print</span><span style="color:#24292E">(</span><span style="color:#032F62">"chip_config.yaml was not found in this folder."</span><span style="color:#24292E">)</span></span>
<span class="line"><span style="color:#005CC5">    exit</span><span style="color:#24292E">(</span><span style="color:#005CC5">1</span><span style="color:#24292E">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#005CC5">print</span><span style="color:#24292E">(</span><span style="color:#032F62">"Config loaded successfully."</span><span style="color:#24292E">)</span></span></code></pre> <p>Walk through it:</p> <ol><li><strong><code>try:</code></strong> — “attempt the following, but be ready to handle failures”</li> <li><strong><code>except yaml.YAMLError as e:</code></strong> — if the YAML parser throws up, print a friendly message instead of a scary stack trace</li> <li><strong><code>except FileNotFoundError:</code></strong> — if the file does not exist, print a different friendly message</li> <li><strong><code>exit(1)</code></strong> — stop the program with an error code so build systems (Jenkins, Make, etc.) know it failed</li></ol> <p>The line <code>exit(1)</code> matters in a CI pipeline — without it, later stages might run on a missing config and produce garbage results.</p> <hr/> <h2>Handling missing keys</h2> <p>A file can be valid YAML but still missing a value your script expects. For example, a teammate hands you a config without <code>frequency_mhz</code>. Reading goes fine, but <code>config['frequency_mhz']</code> crashes with <code>KeyError</code>.</p> <p>Use <code>.get()</code> with a default for optional values:</p> <pre class="shiki github-light" style="background-color:#fff;color:#24292e" tabindex="0"><code><span class="line"><span style="color:#24292E">freq </span><span style="color:#D73A49">=</span><span style="color:#24292E"> config.get(</span><span style="color:#032F62">'frequency_mhz'</span><span style="color:#24292E">, </span><span style="color:#005CC5">100</span><span style="color:#24292E">)   </span><span style="color:#6A737D"># defaults to 100 if missing</span></span></code></pre> <p>For required values, check explicitly and give a helpful error:</p> <pre class="shiki github-light" style="background-color:#fff;color:#24292e" tabindex="0"><code><span class="line"><span style="color:#D73A49">if</span><span style="color:#032F62"> 'chip_name'</span><span style="color:#D73A49"> not</span><span style="color:#D73A49"> in</span><span style="color:#24292E"> config:</span></span>
<span class="line"><span style="color:#005CC5">    print</span><span style="color:#24292E">(</span><span style="color:#032F62">"Error: chip_name is required but missing from chip_config.yaml"</span><span style="color:#24292E">)</span></span>
<span class="line"><span style="color:#005CC5">    exit</span><span style="color:#24292E">(</span><span style="color:#005CC5">1</span><span style="color:#24292E">)</span></span></code></pre> <p>This saves hours of debugging later — instead of a cryptic crash deep inside your tool, the person running the script sees exactly what they forgot.</p> <hr/> <h2>Who writes this code?</h2> <p>In almost every team, the <strong>tools or CAD team</strong> writes the wrapper script once, and every engineer reuses it. You usually won’t write error-handling code yourself. But when the error message appears on your screen, you now know:</p> <ul><li><em>Which file</em> caused it (because the wrapper printed the name)</li> <li><em>Which line</em> (because the YAML error includes it)</li> <li><em>That the problem is in your YAML</em>, not in the EDA tool</li></ul> <p>That knowledge turns a five-hour debugging session into a five-minute fix.</p> <hr/> <h2>Key takeaways</h2> <ul><li><code>yaml.YAMLError</code> is raised for syntax problems (tabs, missing colons, bad quotes) with a line and column number</li> <li><code>FileNotFoundError</code> is raised when the path is wrong — usually a typo or wrong working folder</li> <li>Wrap <code>yaml.safe_load()</code> in a <code>try / except</code> block and print friendly messages</li> <li>Use <code>config.get('key', default)</code> for optional values, explicit checks for required ones</li> <li>Good error handling turns cryptic failures into actionable messages — it is the single highest-value habit when working with config files</li></ul> <p>You now know the complete Python–YAML workflow used across semiconductor design teams: <strong>read, write, and recover gracefully from bad input.</strong> Combined with the YAML-writing skills from Parts 1–3, you can read, write, maintain, and debug any configuration file you are asked to touch.</p> <hr/> <!> <!> <hr/> <div class="not-prose" style="margin-top:2rem;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:1rem;"><a style="display:inline-flex;align-items:center;gap:0.5rem;padding:0.65rem 1.25rem;border-radius:8px;font-size:0.82rem;text-decoration:none;border:1px solid var(--color-border);color:var(--color-text-muted);font-family:var(--font-ui);transition:all 0.25s ease;">← Previous</a> <a style="display:inline-flex;align-items:center;gap:0.5rem;padding:0.65rem 1.25rem;border-radius:8px;font-weight:600;font-size:0.85rem;text-decoration:none;background:var(--color-accent);color:white;font-family:var(--font-ui);transition:all 0.25s ease;">← Back to Home</a></div>`,1);function p(e){var p=f(),m=i(n(p),2);d(m,{minutes:7,learn:[`The two errors you will actually hit: YAMLError and FileNotFoundError`,`How to read the line/column numbers in an error message`,`Wrapping yaml.safe_load() in try/except for friendly failures`]});var h=i(m,78);l(h,{});var g=i(h,2);u(g,{tag:`yaml-lesson-14`});var _=i(g,4),v=t(_),y=i(v,2);a(_),r(()=>{o(v,`href`,`${c??``}/lesson-13`),o(y,`href`,`${c??``}/`)}),s(e,p)}export{p as component};